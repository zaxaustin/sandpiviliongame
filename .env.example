
# Phase 3 — optional. Without these, the Library reads from the bundled
# src/game/data/seed.js and the café's notice board doesn't load; nothing
# else breaks. Get both from your Supabase project's API settings.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
