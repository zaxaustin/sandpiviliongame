Dev log — 2026-07-26
====================

Morning session. Two real-use findings from the user actively adding books +
walking the Learning Tree, both actioned (BETA-FEEDBACK Round 5, #35/#36).

#36 THE STACKS — a Library that scales (user chose "sections -> subsections")
----------------------------------------------------------------------------
Flat 9-tradition shelves + one Index list don't hold a mass of books. Built a
hierarchical catalog: openCatalog/catalogOpen/renderCatalog (catalogOv), reached
from the Index ("🗂 Browse by section"). Section -> Subsection -> books, driven by
LIBRARY_TAXONOMY (tradition -> {section,sub}) with a `category` fallback, so
re-sorting is a data edit not code. Section names are attr/JS-safe via
encodeURIComponent tokens (encTok) + decode in catalogOpen. Seeded taxonomy:
Religion & Spirituality {Buddhism, Hinduism, Hindu Tantra, Daoism, Indigenous,
Practice & Cross-tradition}, Philosophy & Classics, Science, Non-fiction,
Fiction, Other. Good news found in the data: two axes already existed (tradition
+ category; seed CATEGORIES already had fiction/non-fiction/research) — so this
extends, not restarts. TRADITIONS gained Fiction + Non-fiction so the add-a-book
dropdown offers them (no physical shelf needed — they live in the catalog + Your
Shelf; SHELF_HUE/shelfTraditionFor have fallbacks). Physical shelves untouched.

#35 LESSONS AS REAL TOOLS — action-linked (user chose "both, action-linked first")
---------------------------------------------------------------------------------
Lessons felt like static checklists. Steps can now carry action:{label,fn}; the
lesson view renders a button that opens the REAL tool (event.stopPropagation so
it doesn't tick the step). Wired: beating-heart "Connect" -> openConnections;
first-book "Caravan Desk" -> openReviewQueue, "Go read it" -> openCatalog;
meet-residents "Plan a day" -> openCalendar; electronics-101 "Test in the Lab" ->
openScienceHall, "Keep notes" -> openResearchDesk. The lesson now DRIVES the app.
Still to do: the Academy tutor inside a lesson (teach/quiz/answer) — the deeper
half, PAVILION-ACADEMY-PLAN Phase 1.

Verified throughout: node --check + smoke (9 scenes, 22 docs) + build clean.
Docs: BETA-FEEDBACK #35/#36 marked; MANUAL gains The Stacks; this log.

SELF-LEARNING JOURNAL — research + design + The Log (Phase 1)
------------------------------------------------------------
Big ask: make the Pavilion a daily-use bullet journal + second brain you can
TALK to — capture ideas, AI organizes/connects them AFTER, a log of things to do
tackled one by one, pick by mood/energy ("drawing today, electronics tomorrow"),
a backlog of projects to log into, eventually community projects (feed the
hungry / get power) — personal interface rock-solid FIRST. Plus: the daily-use
unlock is an interactive VOICE assistant that's playful AND professional.

Did the homework (WebSearch): why people abandon to-do/calendar apps (dumping-
ground death spiral; fragmentation; reorganizing-instead-of-doing; passive not
proactive; people change) and what works (Bullet Journal rapid logging +
migration; Building a Second Brain CODE+PARA, capture-first with an inbox;
energy/spoon-based selection). Distilled 6 design rules; the load-bearing one:
DON'T FRAGMENT — the Pavilion already has 5 places thoughts land (Writing Desk,
Idea Jar, Notes, sparks, Course Board/Research Desk); the job is to UNIFY them,
not add a 6th.

Wrote plans/SELF-LEARNING-JOURNAL-PLAN.md (research-grounded, unify-focused,
7 phases, with sources cited). Roles made coherent: Log=capture inbox;
Writing Desk=today; Notes=second brain; Threads=projects/interests; Sebastian=
the playful+professional assistant.

BUILT Phase 1 — The Log: reframed the Idea Jar into a universal bullet-journal
capture. LOG_KINDS (💡 idea / • to-do / — note / ✦ to-learn); entries gain a
`kind` (old entries read as idea); setLogKind picker; title/menu now "📓 The
Log"; still zero-friction from the HUD 💡. MANUAL updated. Verified: check +
smoke + build clean.

Recommended NEXT build: voice-in (SpeechRecognition, desktop-first) + replies in
Sebastian's voice — the daily-use unlock the user keeps naming. Then Phase 2
(AI organizes the Log after capture), Phase 3 (Threads backlog), Phase 4 (mood
picker — the anti-abandonment surface).

WRITING DESK = BULLET JOURNAL (iron-out, not sprawl); vision captured
--------------------------------------------------------------------
User clarified the soul (origin epiphany: "why am I doing this alone? build a
place others can come learn/do too" -> a catalog of plans others make; heart-
subject = spirituality; also wants Spanish/coding; a game that works, needed with
AI). Captured in memory (project_platform_and_conductor). Strong steer: IRON OUT
what we have before adding more.

Built (the headline ask, additive/safe): the Writing Desk's white paper is now a
bullet journal — day.log with the three classic entries • task / ○ event / — note
(BUJO_KINDS), rapid-log input (Enter to add), tasks toggle done, remove; rendered
into a new #planLog on the page above the freeform paper (which stays, rows 14->
10). plannerDay patches log:[] for old days. renderPlanLog + setPlanLogKind/
addPlanLogEntry/togglePlanLogTask/removePlanLogEntry; called from openPlanner;
window-registered. Verified: check + smoke + build clean.

Decisions recorded in SELF-LEARNING-JOURNAL-PLAN.md (Update 2026-07-26):
- The Log (HUD capture inbox) vs Writing Desk log (today) — plan a "send to
  today" bridge so capture flows to the day, no parallel silo. Not built.
- Second brain: keep growing My Notes in-house; offer OPTIONAL Obsidian vault
  export/link later (local markdown = aligned, but build-ours-first). Not built.
- Sebastian modes ("ease in: one thing a day" vs "work mode: keep the schedule")
  — bounded next build (data.settings.sebMode + day-read/advisory line).
- Voice: DEFERRED on purpose until the user has a mic + understands the backend
  (their stated reason). Journal works fully typed.
Recommended next small build: Sebastian modes.

SEBASTIAN MODES + NOTES AS BULLET-JOURNAL COLLECTIONS (adding life, iron-out)
----------------------------------------------------------------------------
User: give Sebastian the two paths (adds life), and put the bullet-journal
system into Notes too. Both built (additive, no new silos):

- Sebastian modes: data.settings.sebMode ('ease' default | 'work'); sebModeBlock()
  appends a stance to his prompt — Ease in = commit to ONE meaningful thing,
  light; Work mode = a real timed schedule, held to kindly-but-firmly. Toggle
  buttons (🌱 Ease in / 🎯 Work mode) in his chat quick-actions; setSebMode
  re-renders them. Fallback 'ease' so old saves need no migration.
- Notes = BuJo collections: each note gains an optional note.log of • task /
  ○ event / — note (same BUJO_KINDS/bujoSig as the Writing Desk), rendered under
  the freeform body in the note editor; tasks toggle done. noteLogHtml +
  setNoteLogKind/addNoteLogEntry/toggleNoteLogItem/removeNoteLogItem. Makes a
  note a living topical collection, not just text — My Notes stays the in-house
  second brain, now living.
Handlers registered. Verified: check + smoke + build clean. Docs: plan Update
marked both BUILT; MANUAL updated (Sebastian paths + Notes collections).

MANAGE MY LIBRARY — fix duplicates + move misfiled personal books
-----------------------------------------------------------------
Real-use problem while the user added books: doubles (Paradise Lost x2) and
books on the wrong shelf, with no in-game way to fix either (removePersonalBook
existed but only via opening each book in the Reader; no re-file at all).
Built openManageLibrary/renderManageLibrary: lists all data.personalLibrary
(👤) books, each with a "Move to" shelf dropdown (TRADITIONS, now imported into
overlays) -> movePersonalBook(slug,tradition) sets b.tradition (re-files on the
shelves AND in The Stacks at once, since both sort by tradition), an Open, and a
Remove. Same-title duplicates flagged (border + "possible duplicate" badge).
removePersonalBook gained a `from` arg so it re-renders the manager instead of
jumping to the shelf. Entry point: "⚙ Manage my books" on Your Shelf; new
manageLibOv overlay. Certified books untouched (managed via the review pipeline).
Verified: check + smoke + build clean. MANUAL §4 updated. Also confirmed to the
user: I can't see their local library count (it lives on their machine — the
privacy feature); The Stacks shows the live total. Obsidian decision: keep
building ours (My Notes + collections), optional markdown export bridge later.

NO-SOURCE PERSONAL ADDS — Your Shelf needs no license/provenance
----------------------------------------------------------------
User: I want to add books to my PERSONAL shelf even with no source / unknown
copyright (my own copies, not shared). The code already allowed it (shelveManual
FormNow requires only title+body; shelveAsPersonal defaults blank license to
"Personal — license not certified") — the friction was framing + two real gaps:
- fillDropForm routed UNKNOWN-source drops to a Research Desk "Caravan Drops"
  note instead of the form, blocking the legitimate "add my own book" case. Now
  an unknown source FILLS the form (title+body) so you can pick a shelf and Add
  to Your Shelf; message says no license/source needed for personal.
- The add-form shelf dropdown was hardcoded to 6 traditions — now uses all
  TRADITIONS (Fiction/Non-fiction/Hindu/Native American/Tantra included).
- Copy/labels updated: License/Source marked optional for Your Shelf; form intro
  + drop-zone copy say a personal copy needs no provenance; "Not listed" now
  means "goes to Your Shelf," not "filed away in Research Desk."
Kept the wall firmly on the SHARED review queue (certified commons still needs a
real redistributable license). MANUAL §4 + PROTOCOLS Protocol 1 provenance rule
clarified with the personal exception. Verified: check + smoke + build clean.
(unrecognizedDropProject now unused; left in place, harmless.)

LOG -> TODAY BRIDGE (closing the capture->organize seam)
--------------------------------------------------------
User picked this. sendLogEntryToToday(id): a Log entry MIGRATES onto today's
bullet-journal page (day.log) — todo/learn -> • task, else -> — note — and is
REMOVED from the Log (true BuJo migration, keeps the inbox clean, anti-dumping-
ground). "→ Today's page" button on each Log entry; Log meta hints it; window-
registered. Now capture (HUD Log) flows to the day in one tap instead of being a
parallel list — the one open seam from the journal plan, closed. Verified: check
+ smoke + build clean. Plan Update marked BUILT.
Also noted to the user: the "upright in the public square" value (don't take away
people's ability, but stay honest in the commons) — the personal-vs-commons split
embodies it; aligns with existing no-poison/truth-seeking ethos.

AI BOOK ANALYSIS — the local AI actually engages with a book (fun use)
----------------------------------------------------------------------
User wanted a way to get an overall impression of a book or an analysis of a
chapter, giving the local AI real use. Built in the Reader (the natural home —
you open a book there, incl. from the inventory):
- "✨ Overall impression" (summary view): AI reads the book's summary + sections
  and writes a thoughtful impression. aiBookImpression().
- "✨ Analyze this page" (full-text view): AI does a close read of the CURRENT
  page's text. aiAnalyzePage(). ("chapter" ~= the current page, since full text
  is paginated.)
Both saved as book notes via saveAiBookNote, prefixed "✨ ... (AI)" so they're
never confused with the user's own notes; they get the existing → Bring to
today's plan + search for free. Neutral/analytical voice (WORK_CHARTER + "a
perceptive literary companion") so it serves any book on its own terms. Bounded
on purpose (impression = summary+sections; analysis = one page) to respect a
local model's context — never feeds the whole book. Buttons show only when AI is
active (rdImpressionBtn in openReader, rdAnalyzeBtn in renderFullTextPage, both
independent of TTS). isEmptyReply retry-guard messaging. Verified: check + smoke
+ build clean. MANUAL §4 updated.

LISTEN WHILE YOU JOT — read-aloud survives opening Your Notes
------------------------------------------------------------
User: use the phone/pocket idea with books — listen, minimize, go to notes, jot,
see notes in-game. Honest finding: you can ALREADY listen + jot on a book in the
Reader (the note box is there; audio doesn't stop while you type). The gap was
leaving the Reader for the separate notes screen — closeUI/Esc calls stopSpeaking.
Safe fix (no touching the ~13 stopSpeaking sites): openNotesLog never stops audio,
so added a Reader button "🗒 My notes (keeps playing)" -> openNotesWhileListening()
sets state.audioReturnSlug + opens Your Notes with audio still going; a
tap-to-return "▸ back to the book" banner (returnToBook -> openReader) shows in
the notes log; closeUI clears audioReturnSlug. So: listen -> open all your notes ->
jot -> audio keeps playing -> tap to return. Verified: check + smoke + build clean.
MANUAL §4 updated.
Honest limits (offered as the bigger Reading-Room next build, not done): read-aloud
is still per-page/one-utterance (no continuous auto-advance yet) and there's no
global floating audio player that roams EVERY screen — both are READING-ROOM
Phase 0-1 (auto-advance + keep-alive + a persistent mini-player). Left for when
the user wants it, to avoid re-engineering the TTS lifecycle late in a session.

VISUAL POLISH — plan + a viewable mockup (can't see the live game from here)
---------------------------------------------------------------------------
User: make the visuals more appealing/professional — "a game that's not a game."
Reconciled with the sleeper-car standing decision up front: professional =
cohesive & INTENTIONAL, not high-res; nothing here spends CPU/GPU. Read the real
style.css: coherent amber-on-dark-parchment + Courier mono, good bones (shadows,
card/badge/phone patterns, reader = ink-on-parchment); what holds it back = no
design tokens (hexes hardcoded in style.css AND inline in overlays.js),
slightly inconsistent radii/borders/spacing, mono-everywhere (a type-pairing is
the biggest cheap "pro" lever), minimal world art.
Wrote plans/VISUAL-POLISH-PLAN.md: 5 phases, cheapest-first — (1) design tokens
(:root palette + radius/spacing/type scales; start as a zero-visual-change
refactor, then tune from one place), (2) component consistency + hover/focus/
motion, (3) typography (mono for labels + a warm serif for body, web-safe/offline),
(4) world & sprites (palette-locked, ambient light, title screen; pairs with
CHARACTER-CUSTOMIZATION-PLAN), (5) motion & touches. Method stated honestly: I
can't see the running game, so propose visually -> user approves -> apply to real
CSS in small verifiable steps, never a blind restyle.
Built a VIEWABLE MOCKUP (Artifact, since I can't screenshot the game): a faithful,
tokenized style guide in the Pavilion's own palette (swatches, the two-face type
specimen, and mocked real components — panel, bullet-journal log, cards, badges,
the phone chip, an ✨ AI book note, the parchment reader). Single-world dark by
deliberate choice. URL: claude.ai/code/artifact/47283baf-0c83-4767-8d5b-f4bb36283d6f
Nothing in the live game changed yet — awaiting the user's read on the direction.
Recommended first real step: Phase 1 tokens (safe).

---

Later — three reader fixes from real use (user feedback):
1. "Can't close the book and walk around, like the phone." True gap: chats had
   a pocket phone; the reader had none. Added a BOOK POCKET mirroring
   minimizeChat/restoreChat — 📕 Pocket & walk (in both the summary and full-text
   rows) closes the reader, clears state.ui so you can roam, and leaves the
   read-aloud running. A small card sits bottom-LEFT (own #bookPhone, so it never
   overlaps the chat phone / butler ping on the right), pulses while reading, taps
   to return, ✕ to put down. A 1s interval keeps its "🔊 reading…" status honest.
   Session-scoped (lives on state, not data). openReader retires a stale pocket.
2. "How do I see book review notes / analyze a page?" The ✨ Overall impression
   and ✨ Analyze this page buttons were HIDDEN whenever no AI was connected —
   invisible feature. Now always shown; clicking without a model gives the
   existing "connect a local AI (⚙ Manage AI connections)" nudge instead of the
   button silently not existing. Their output still saves into the book's own
   notes list at the bottom of the reader (marked ✨).
3. "Review my old notes." Already existed (pause menu → 🗒 Your Notes, and the
   reader's 🗒 My notes button opens the same searchable log) — this was
   discoverability, addressed in the MANUAL rather than code.
Verified: node --check + smoke (9 scenes, 22 docs) + build all green. MANUAL updated.

---

Notes, unified (user: "the note table should be integrated to Your Notes, make a
new one there, and link a note to a book in my inventory — to a page or a chapter"):
- Your Notes is now a real WORKSPACE, not just a gathered read-only view. ＋ New
  note creates a data.notes note and opens an inline editor (renderNotesLogEditor);
  your own notes get "✏ Open to edit / link a book" from their expanded card.
  Same data.notes the Writing Desk's My Notes drawer uses — one store, two doors.
  Note-log mutators now re-render through refreshNoteEditors() so they work from
  either editor.
- BOOK LINKS on a note: note.book = {slug, page, chapter}. noteBookLinkHtml()
  (shared by both editors) links a note to a book you're CARRYING (data.inventory),
  optionally pins a page (number) and/or a chapter (a doc.sections heading), and
  "▸ Open the book" jumps straight there — page → full text at that page; else the
  reader's chapters. A 📖 chip shows the link in both note lists. gatherNotes()
  carries n.book/n.id for My Notes so the hub can show/edit it.
- Broader direction the user set: "clean up the rooms — not just aspects of a game
  but useful tools for daily life; this front end is what other people will use."
  Notes was the room they detailed, so it went first. Next candidates, room by room:
  the Writing Desk (today's page), the Library/Reader, the Café/desks. Awaiting a
  steer on order.
Verified: node --check + smoke (9 scenes, 22 docs) + build all green. MANUAL updated.

---

Library / Reader — digest books better + find the lessons (user asks):
- "Ask Sebastian a question about the book while I'm reading." Added
  💬 Ask about this book (summary view) and 💬 Ask about this page (full text) in
  the Reader. Reuses the whole resident chat stack via openChatDialog, then hands
  Sebastian the book as grounding (state.dialog.book) the same way a folder review
  is handed over — summary+sections from the summary view, the EXACT current page
  from the full text. Sebastian's systemPrompt gets a focused reading-companion
  branch when a book is set (skips the day-read noise). No AI connected → a plain
  "connect a local AI" nudge. Pocket the chat to keep reading while he thinks.
- "Where are the lesson plans?" The Learning Tree was buried inside the Course
  Board overlay only. Added 🌳 Lesson plans (Learning Tree) to the pause menu.
  Adding your own path = have Sebastian/Quill/Computer draft a course → pin to the
  Course Board (existing flow), documented in the MANUAL.
- "Look at / take notes on the book I'm holding": already works — Inventory
  (pause menu 🎒) → tap a carried book → the full Reader opens with the note box,
  ✨ impression/analyze, and now 💬 Ask. Clarified in the MANUAL, no code needed.

Backend status (answered live, 2026-07-26): sand-pavilion-minio container Up 8h,
127.0.0.1:9000-9001, /minio/health/live → 200. So the local Docker/MinIO storage
is running. The deliberate split still holds (README "where book data actually
lives"): full book TEXT → local MinIO in Docker on this machine (100 GB reserved,
a few MB used); catalog card (title/license/source/summary/search) → Supabase, or
the local seed when Supabase isn't configured; PERSONAL books → plain files in the
desktop app's own data folder, no services at all. Cloud never holds the text.
Verified: node --check + smoke (9 scenes, 22 docs) + build all green. MANUAL updated.

---

Lessons → Notes → AI lesson plans (user: "have lessons in the codebase, get them
out into notes, then a pathway for the local AI to make lesson plans out of them;
if it can't handle that we'll figure something else out"):
- Lessons stay in the codebase (CURRICULUM + lessons/*.md). Added two pathways OUT
  on the lesson view: 📓 Save to my Notes (plain, no AI, always works — dumps the
  lesson outline into data.notes) and ✨ Draft a lesson plan (AI) (WORK_CHARTER;
  turns the lesson into a goal → timed sessions → hands-on task each → self-check,
  saved as a ✨ note). Both land in the unified Notes hub.
- General pathway too: ✨ Make a lesson plan from this note (AI) in BOTH note
  editors (noteToolsHtml) — so a saved lesson (or any note) becomes a plan. Exactly
  the flow the user described: lesson → Notes → AI plan.
- Graceful degradation everywhere: no model / empty reply / dropped connection each
  give a plain message and point back to the no-AI save.
- CAN the local model handle it? Tested live: llama3.2:latest (3B) produced a
  well-structured, usable plan in ~10.5s (mild spec drift — invented "voice
  commands" — which is why plans save as EDITABLE notes to review, not gospel).
  Bigger installed models (qwen3.5:9b, deepseek-r1:8b) available if wanted. Answer:
  yes, even the small local model handles it. Installed models seen: llama3.2,
  deepseek-r1:8b, qwen3.5:9b, deepseek-v4-pro:cloud.
Verified: node --check + smoke (9 scenes, 22 docs) + build all green. MANUAL updated.

---

Readiness check on notes + scheduling (user: anything glaring / more time-tested,
wants to show friends). Honest audit found ONE genuinely glaring, time-tested gap:
bullet-journal MIGRATION was missing. openSparks() carried reading-sparks forward
but the daily LOG's unfinished • tasks did not — a task jotted today silently sank
into a past day tomorrow, invisible. That's exactly the cornerstone ritual Ryder
Carroll's Bullet Journal turns on.
BUILT: migratableTasks() gathers unfinished task-kind log entries from days before
today; the Writing Desk's log now shows "↩ N unfinished tasks from earlier" at the
top with → today (migrate), ✕ (let go = mark done on its own day), and → Carry all
to today. Only tasks migrate (events/notes are date-anchored). Mirrors the existing
sparks carry-forward. MANUAL updated.
Otherwise: notes are strong (unified hub, create/edit anywhere, folders/tags, book
links, per-note BuJo logs, lesson plans); scheduling is strong (intention/ember,
the daily BuJo log, rhythm blocks, sparks, calendar with timed events + reminders,
Sebastian day-planning). Verdict: demo-ready. Verified: check + smoke (9 scenes, 22
docs) + build all green.

---

Library second floor + bulk personal import (user asks; friend test: liked it,
visuals need work — noted for VISUAL-POLISH-PLAN, not this pass).

Second floor:
- New scene buildLibraryFloor2() (scenes.js) — W16xH11, two shelf rows (four
  blocks): Hindu (front-west), Native American (front-east), Fiction (back-west),
  Non-fiction (back-east). Signposted spiral staircase at the back-east of the
  ground floor (warp {14,3}->libraryfloor2 {8,8}; a "A SPIRAL STAIRCASE" sign at
  {14,2}); floor2 down-warp {8,9}->library {14,4}. Landing/shelf signs upstairs;
  a reading nook + a candle. Quill's line updated to point up the stairs. Unlike
  the secret basement, this is meant to be found.
- shelfTraditionFor() gains a libraryfloor2 branch (same row/side math). SHELF_HUE
  gains Hindu/Native American/Fiction/Non-fiction (+Tantra) so spines are colored.
  These four were the TRADITIONS with no physical shelf yet — now they have one.
- Smoke test now reports 10 scenes (was 9).

Bulk import (many at a time):
- The drop handler was files[0] only. Refactored: handleBookDrop + a new file
  picker (handleBookFilePick, hidden <input multiple>) both funnel through
  intakeBookFiles. ONE file -> fills the form (fillFromSingleFile). MANY files ->
  bulkShelveDroppedFiles: parse each (parseBookFile, shared), shelveAsPersonal on
  the chosen #rmTradition shelf, progress line, per-file failures collected not
  fatal. Personal copies need no license, which is what makes a batch safe. Drop
  zone copy updated ("one, or a whole pile at once") + "📁 Choose files…" button.
Verified: check (overlays+scenes) + smoke (10 scenes, 22 docs) + build all green.
MANUAL updated (Library second floor; bulk add).

---

Visual revamp — plan reframed + Phase 0 executed (user: interface is what's
lacking, make it pro-game-like; raised a stack question).

Stack question settled in the plan (VISUAL-POLISH-PLAN.md §0): KEEP the current
stack, don't rewrite. It's vanilla JS (not TS) on Vite, already running as BOTH
the Electron desktop app AND the live web app — so "one day an interactive
website" is already true. Java would be desktop-only, throw away everything, and
not help the local-first/local-AI story at all. "More game-like" is a craft
problem (the HTML/CSS overlays + the cheap canvas world), solved in-stack, never
a language change.

Plan is now a sequenced "execute one by one" revamp: Phase 0 shelf organization
(done), 1 design tokens (foundation, invisible), 2 interface chrome (the big pro
jump — panels/buttons/cards/badges, hover/focus/spacing), 3 typography (two-face
system), 4 world/sprites/title, 5 motion. Honest method kept: mockup → react →
small verified CSS steps.

Phase 0 BUILT: shelves now sort alphabetically (leading the/a/an ignored) and
break into bays of <=20 with letter-range captions (A–D · 1–20). Sort at open, so
state.shelfDocs is the ordered list and selectBook/shelfNav indices stay honest.
By TITLE for now — no stored author field yet; noted as a small follow-up to add
`author` and flip shelfSortKey to by-author. CSS: .shelfRow/.shelfRowCap added.
Verified: check + smoke (10 scenes, 22 docs) + build all green. Next: Phase 1
tokens (or Phase 2 chrome) — awaiting the user's go on order.

---

Visual revamp Phase 1 — design tokens (zero visual change). style.css now opens
with a :root token block (palette: surfaces/ink/gold/edges/accents; type; spacing
--sp-1..6; radius --r-sm/md/lg; two shadow tokens), each set to the EXACT value
components already used. Core components repointed at tokens: body, .panel (+h2/
h3/meta/p), .btn/.ghost, .xbtn, .card (+.t/.s/hover), #dropZone.over, .badge
(+.lic), a.link, inputs/textarea, label. Nothing changed on screen — the look is
just tunable from one place now. Left literal for Phase 2 (intentional
normalisation later): odd radii (7/8/10px), paddings, and one-off hexes in
#dialog/#hud/chat/phone cards/calendar/blocks/spines.
Also: user's art constraint recorded in the plan (Phase 4) — keep the warm
walk-around RPG feel, but ALL original art, resembling no existing game's IP, and
NO creature/pet system for now. And created plans/VISUAL-REVAMP-CHANGELOG.md as a
concrete, revertible record of Phase 0 + Phase 1 (the "documented written
changes" the user asked for). Verified: build + smoke (10 scenes, 22 docs) green.
Next: Phase 2 interface chrome — propose via an updated style-guide mockup first.

---

Got EYES on the game (big unlock). Set up a screenshot pipeline: puppeteer-core
(installed in scratchpad, not the project) driving the SYSTEM Chrome against
`vite preview` on :4173 — click #startBtn, closeUI(), then call window.* overlay
fns and capture PNGs I can actually read. Documented in memory
reference_screenshotting_the_game. From now on visual work is verified by looking,
not guessing.
Saw the real UI: the world is genuinely cohesive; overlays render clean; Phase-1
tokens caused ZERO regressions (reader parchment skin, panels, buttons all
correct); Phase-0 shelf bays confirmed (alphabetical, "the/a/an" ignored, letter-
range caption A–S).
Phase 2 (start) BUILT + verified by screenshot: the pause menu was ~19 identical
stacked ghost buttons — regrouped into labelled sections (Your day / Library /
Local AI / Account & data / Save & session) in a 2-column grid, prominent solid
▸ Resume, danger-red Reset. Much more scannable; fits the viewport. CSS:
.menuResume/.menuSection/.menuGrid/.menuItem/.menuDanger. Changelog + plan updated.
Next Phase 2: unify card/badge/button hover+focus states + spacing/radii onto the
scale across the other panels. Good point for the user to review in-app.

---

Phase 2 interaction states + Phase 3 (start) reading serif — both verified by
screenshot (the eyes-on-the-game pipeline).
- Interaction states (style.css): .btn hover brightens (+ smooth transitions,
  keeps press :active); .btn.ghost gets a faint warm wash + brighter edge on
  hover (works for any accent) + subtle :active; .menuDanger a red wash; one
  consistent keyboard focus ring (outline gold-bright) on buttons/links/cards/
  spines; .card:hover now lifts its background too. Whole UI feels responsive now.
- Reading serif (Phase 3): new --font-read (Georgia->Palatino->serif, web-safe,
  no fetch). Reader prose — book title, section headings, summary, full-text page
  — now serif at 15.5px/1.8; all chrome (buttons/meta/notes column) stays mono.
  A book reads like a book. Chose web-safe stack over an embedded @font-face for
  now (zero fetch, ships on Win/Mac, serif fallback on Linux).
Screenshots confirmed: menu hover wash reads clean; reader shows serif prose vs
mono chrome, elegant. Changelog + plan updated. build + smoke (10 scenes) green.

---

Backend direction chosen (user): local Postgres + MinIO, and specifically "put my
drag-drop books in the Docker 100 GB space." Diagnosed the data loss first: since
Supabase left, localStorage IS the DB; personal book TEXT inlined there in the
browser, and a bulk drop blew the ~5 MB quota -> persist() failed -> books gone on
reload. (Desktop app already wrote texts to app-data files; MinIO only held seed
texts, never personal ones.)

BUILT — personal books -> local MinIO (desktop app path):
- Proved the mechanism live first: `docker run minio/mc ... mc pipe
  local/sand-pavilion-library/personal/_selftest.txt`, then curl over
  http://localhost:9000/... -> HTTP 200 with ANONYMOUS read (what the Reader uses).
- electron/main.cjs: desktop-minio-write / desktop-minio-delete IPC handlers using
  the same mc-in-a-container pattern as push-fulltext.py; host via
  host.docker.internal; creds from env or .env.local (MINIO_*), never the renderer;
  key prefix personal/. + minioConfig() loader, mcRun() helper (child_process spawn).
- electron/preload.cjs: exposes minioWrite/minioDelete on desktopBridge.
- overlays.js shelveAsPersonal(): prefers MinIO -> app-data file -> inline; stores
  storage:{bucket,key}; the Reader already loads that over HTTP. removePersonalBook
  cleans up the MinIO object too.
- This moves heavy text OUT of localStorage, so the catalog stays tiny and the
  quota loss is fixed at the root (desktop). Web build unchanged (no bridge).
CANNOT drive Electron from the harness — needs in-app verification: npm run
electron:dev + Docker running, drop a book, confirm read-back + survives restart.
Creds live in .env.local (MINIO_ROOT_USER=sandpavilion, bucket sand-pavilion-library).
Recorded in SELF-HOSTED-STACK-PLAN.md ("Built 2026-07-26"), incl. the user's idea
to ship curated book/lesson PACKAGES in MinIO with the beta.
Verified here: node --check (main/preload/overlays) + build + smoke (10 scenes) green.

---

Visual polish, cathedral mindset (user: make it gorgeous, worthy of unseen
scrutiny; sat with the Ganesha telling first, per CLAUDE.md, before this
undertaking). Also captured the next beta-test round plan in BETA-LAUNCH-PLAN.md
so it isn't forgotten ("packages in MinIO", must-verify list incl. MinIO book
storage + clean-machine install, distribution + feedback loop).

Phase 4 (start) — TITLE SCREEN redesigned as a threshold (index.html #title +
style.css). Warm hearth-glow radial + bottom vignette over near-black; the mono
name as the "sign" (clamp 30-52px, soft gold glow); an ornamental rule with a
centred floret; the old wall-of-text paragraph replaced by a one-line SERIF
invitation (--font-read, italic) — orientation lives on the "New here?" button
now; quiet coloured status lines (green save/blue AI/muted library); centred
button stack, Enter with a faint glow; gentle titleRise fade-in (respects reduced
motion). Verified by screenshot: reads as a real, reverent entrance. build + smoke
(10 scenes) green.

World VIGNETTE added (#vignette): one fixed radial-gradient over the canvas,
below the HUD (z-2, pointer-events:none), softly darkening the corners so the
world reads as composed lamplight. Zero renderer cost. Verified by screenshot —
centre bright, HUD crisp. build + smoke green.

---

World life — breeze + characters + café fire moved outside (user: work on the
characters and the feel of walking, keep the eastern feel, add a breeze, move the
cafe fire outside).
- render.js drawBreeze(): ~14 season-tinted petals drifting on the wind (SPAL
  flower/flower2/canopy), positions from state.time, outdoor-only, called in the
  outdoor block after the day/night gradient. "Blossoms on the wind", cheap.
- render.js drawRobedFigure: shoulder highlight + centre seam (folded-cloth depth)
  + feet that plant when still and stride (alternate) when the walk bob is active.
- scenes.js: café 'hearth' station removed from the interior; overworld gained a
  stations:[{x:22,y:22,kind:'hearth'}] in the plaza beside the path; overworld
  café sign updated to say the hearth is out in the plaza now.
Verified by screenshot: THE HEARTH fire pit visible outdoors near spawn; petals
drifting across the grounds; figures grounded with feet; vignette framing it.
node --check (render+scenes) + build + smoke (10 scenes) green.

Outfits softened (user: "a bit too pointy"): drawRobedFigure sleeves + robe hem
rewritten from straight-line polygons to quadratic curves — draping sleeves with
rounded cuffs, rounded shoulders, a gently flared curved hem. Verified via a 2x
close-crop screenshot (figures read as cloth now, feet grounding them, topknot
cap intact). build + smoke green. Next: the build plan (beta).
