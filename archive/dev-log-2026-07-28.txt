=======================================================================
 SAND PAVILION — dev log, 2026-07-28
 "One question at a time"
=======================================================================

The whole session came out of one sentence:

  "We have a pathway for Claude, but what human needs are simple steps.
   If it's too much for the model we should break up the steps to make
   it more manageable for the AI and the user."

Yesterday's round ended with the lesson-drafting *working* but weak: the
real local model returned a single run-on line and a deliberately
forgiving parser rescued something out of it. I wrote at the time that
"a 9B model's format adherence is unreliable" and treated that as a fact
of life to be absorbed. It wasn't. It was a symptom of asking too much
in one breath.

WHAT WAS ACTUALLY WRONG
-----------------------
One call asked ornith:9b for TITLE, SUMMARY, TRACK, LEVEL and 4-6 STEPS.
It answered the shape it could hold, not the shape it was given. The
live test had been recording this plainly for a day — `steps: 1 lines`,
three checks red — and I'd read it as a parsing problem.

THE FIX, WHICH IS THE SAME FIX FOR BOTH HALVES
----------------------------------------------
The draft is a chain of single questions now: a title, then a summary,
then a track, then each step in turn, each one a one-line answer with
the steps written so far in front of it.

The model finds every ask easy. And the person watching gets simple
steps too, which is the part I would have missed if the request hadn't
named both: the form fills in field by field, the status says "6 of 8"
instead of spinning, there's a Stop button that keeps whatever arrived,
and a stage that comes back badly costs that one field instead of the
whole draft.

Same prompt, real model, before -> after:
    steps drafted        1  ->  5
    live checks passing  7/11 -> 11/11

TWO MORE THINGS ONLY THE REAL MODEL COULD SHOW
----------------------------------------------
Once the shape was fixed, the content faults became visible:

  * It repeated itself. Given steps 1-4 and asked for step 5, it
    re-wrote step 3 -- "Folds hands into a loose fist" then "Folds hands
    into a fist". Exact-match dedupe is useless against that. So
    tooSimilarStep() compares the ACTION half only, drops filler words,
    and calls it a repeat when most of the shorter step's words appear
    in the other. The chain then just asks again; three spare asks are
    budgeted for exactly this.

  * It wrote in the third person -- "Sits comfortably with back
    straight". Fixed in the prompt: address them directly, start with a
    verb.

Both are permanent smoke cases now, with the real captured strings.

THE LIBRARIAN GOT THE SAME MEDICINE
-----------------------------------
Sixty titles in one ask -> eight at a time. That was precisely the
"it answered, but nothing matched your titles closely enough" failure
from yesterday: a small model given a long list files the first few,
invents shelves for the middle and drops the rest. Each batch's picks
are kept even if a later one fails, and the status reports a real count.
Real-model result: 3 of 4 -> 4 of 4.

THE ONE THAT WASN'T ASKED FOR
-----------------------------
While chasing a stray console error in the grove suite I probed the
running app's actual network traffic, and found it calling

    https://<project>.supabase.co/rest/v1/library_documents

on startup, unprompted, every launch.

That is correct for this dev instance and completely wrong for anything
handed to a beta tester. .env.beta already empties every service
variable, so `build:beta` was always clean -- the real hazard was that
ONE mistyped script (`electron:build` instead of `electron:build:beta`)
produces a poisoned installer that looks identical to a good one.

So it's structural now: scripts/verify-beta-build.mjs runs inside both
beta build scripts and FAILS THE BUILD if the bundle carries a Supabase
host, a JWT-shaped string, a Supabase key or MinIO credentials. Proved
both directions -- it rejects a dev build (naming the exact key it
found) and passes a beta build.

Nothing was ever committed; dist/ and .env.local are both untracked.
This was a shipping hazard, not a leak. But it's the kind of thing that
would have been discovered by a tester, which is the wrong person to
discover it.

WHAT THIS SESSION IS WORTH REMEMBERING FOR
------------------------------------------
When a local model gets something wrong, ask it something smaller before
you write a cleverer parser. It is nearly always the better interface
for the human as well -- which is what the request said in the first
place, and I only half heard it.

Also: run the thing, watch its traffic, don't just read it. Both of
today's real findings came from observation, not from code review.

VERIFIED
--------
  npm test                      clean
  npm run build                 clean
  npm run build:beta            clean + verifier passes
  verify:beta on a dev build    correctly FAILS
  13 live puppeteer suites      164 checks, no failures, no page errors
  test-ai-real.mjs              11/11 against real ollama/ornith:9b

STILL OPEN (unchanged)
----------------------
  * The user's own desktop-app run (BETA-PREFLIGHT tiers 0-2)
  * The welcome-packet decision
  * Cut the 0.1.0-beta.2 installer
  * Clean-machine install test
  * #10 -- Course Board / tree as a college index (deferred past beta)


=======================================================================
 SAME DAY, SECOND SESSION — "we are at a good point to cut the beta"
=======================================================================

Answer: we were not, and the reason was the worst one available.

THE PACKAGED APP HAD NEVER BOOTED
---------------------------------
Chasing whether bundled book texts would load in the desktop app, I
booted dist/ the way the INSTALLER boots it -- win.loadFile(), a file://
origin -- instead of the way development does. It didn't boot. There was
no vite.config.js in the project at all, so Vite emitted absolute asset
paths (/assets/index-xxx.js), and under file:// those resolve to the
filesystem ROOT: C:/assets/... Nothing there.

The failure is quiet in exactly the way that keeps a bug alive for
weeks. index.html itself loads, so the window shows the title screen and
"Enter the Grounds". Behind it: no script, no stylesheet, no error
dialog. A button that does nothing.

  index.html loads at all      : true
  THE GAME'S OWN CODE RUNS     : false
  the stylesheet arrives       : rgba(0,0,0,0)

Every desktop check until now went through electron:dev, which loads
from the vite dev server over http and therefore cannot see this. The
one gap that mattered most, hidden by the one convenience that made
testing easy. The 11 July beta.1 installer was almost certainly a dead
app -- which fits the record exactly: the clean-machine install test is
the one thing that was never done.

Fixed with base:'./'. Relative paths work under file:// and http alike,
so Vercel is unaffected (39/39 web pre-flight still green). Guarded by
test/live/packaged-boot.cjs, which boots dist/ through a real Electron
main process using the installer's own preload, sandbox and loadFile,
and fails if the code didn't run, the stylesheet didn't arrive, the
bridge isn't wired, Start doesn't start, or a panel comes up empty.

THEN THE ARTIFACT ITSELF, ONCE ANYONE LOOKED
--------------------------------------------
  * The build log had been saying "default Electron icon is used" for
    weeks. Every tester's taskbar would have shown the Electron atom.
    There was no icon asset at all. Drew one instead of downloading one:
    scripts/make-icon.mjs renders a 32x32 pixel-art pavilion (moon
    behind, dune below), scales it by whole numbers to 256/128/64/32,
    encodes the PNGs by hand with node:zlib and wraps them in an ICO. No
    dependencies, colours straight out of style.css. Integer scaling is
    the point -- it stays crisp at every size Windows asks for.

  * The shipped app.asar was 521 node_modules entries to 5 dist ones.
    electron-builder packs everything in `dependencies`, and
    supabase-js was listed there even though the Electron main process
    never requires it. It's a bundle-time dependency. Moved to
    devDependencies: 530 entries -> 9.

AND THE LAST HONEST GAP IN THE LIBRARY
--------------------------------------
Ten of the seed books -- Meditations, The Republic, the Tao Te Ching,
Origin of Species and six more -- ship as a Pavilion SUMMARY. They had
no "Read the full text" button and said nothing about why, so opening
Marcus Aurelius gave an unexplained precis and no way to tell whether
the app was broken or that was all there was. Silence is not honesty.
Each now says plainly: this is the summary, the text is public domain,
here is the source, drag the file onto this window and this page becomes
the real book. The Pavilion's own seven writings never show it -- they
are complete as they stand and aren't missing anything.

beta.2 IS CUT
-------------
release/Sand Pavilion Setup 0.1.0-beta.2.exe, 94.97 MB, and it is the
first installer ever verified as an artifact rather than as source.

THE THREAD RUNNING THROUGH BOTH SESSIONS TODAY
----------------------------------------------
Morning: when a local model gets it wrong, ask it something smaller.
Afternoon: test the artifact you ship, not the one you develop in.

Both of today's worst findings -- the Supabase call on startup and the
dead packaged app -- were invisible to every existing test and obvious
within minutes of running the real thing and watching what it did.

VERIFIED
--------
  npm test                      clean
  npm run build:beta            clean + verifier passes
  packaged-boot.cjs             6/6, real Electron 43, file://
  preflight.mjs                 39/39, no external requests
  app.asar                      9 entries, no keys, no cloud host

STILL OPEN
----------
  * The clean-machine install test (genuinely un-automatable)
  * Ten unbroken minutes listening to a real book
  * Copyright triage over the real 128 books
  * #10 -- Course Board / tree as a college index (deferred past beta)


=======================================================================
 SAME DAY, THIRD SESSION — getting it ready to hand over
=======================================================================

Three questions, and the last one turned out to be a confirmation.

GUIDES
------
"Any other guides we should put into the game anyway?" -- yes, three,
and the gap was real: everything a newcomer stumbles on was FINDABLE in
the interface, but nowhere could you sit down and read it.

  Waking the Residents -- a local AI explained to someone who has never
    installed one, opening with the fact that they don't need it.
  Filling Your Own Shelves -- drag a file on the window; where texts
    actually live; why your own shelf asks you for no licence.
  When Something Goes Wrong -- written for a beta tester, ending with
    the only thing this project cannot do for itself: tell us.

Seed library 24 -> 27.

THE BACKEND, REFRAMED
---------------------
"I'm fine spending a bit of money to rent a server... we can minimise
what to communicate." Taking that last clause seriously IS the plan.

The unit is the packet, not the user. The server does not need to know
who anyone is -- it needs to hold packets and list them. That deletes
accounts, which is where all the cost, risk and liability live, and
replaces them with a keypair per Pavilion. Three endpoints, content-
addressed by SHA-256, reading open to anyone.

Phase 1 is a read-only static mirror on a ~EUR4.50/month box: no
server-side code, no database, no auth. Later the same folder gains an
index.html and becomes the WEBSITE he asked for -- one artifact, two
audiences, a person browsing guides and a Pavilion pulling data. Phase 0
(files by hand) keeps working forever, so nothing is ever the only copy.

"CAN MY COMPUTER RUN THIS?"
---------------------------
"Most of my friends don't even have Ollama or know how to check their
computer's capabilities, most have laptops."

The tiered guide already in the app was honest and useless to exactly
the person who needed it: it said "8 GB RAM -> this model", which
assumes you know your RAM. So the app measures now -- Manage AI
connections -> Check this computer -- and answers in plain words with a
copyable pull command. Laptop advice included, since getBattery() can
tell.

The test earned its keep immediately. A rule refusing any model over 60%
of memory failed on a 4 GB machine offered a 1.3 GB one. The right fix
was not a smaller model but an honest verdict: below ~6 GB there is now
no recommendation at all -- "skip the AI on this machine, and lose
nothing that matters", no command, and a pointer to what this place is
actually for.

AND THE CONFIRMATION
--------------------
"I can't use the Sand Pavilion app on my computer."

That is finding #17 arriving from the other direction. The app-data
directory settles it beyond argument: localStorage holds saves under
localhost:5173 and NOTHING under file://. The installed copy was 0.0.1
from 8 July, and it never booted, so it never wrote a save. The dev app
has been the only working Pavilion all along -- which is exactly why it
felt fine while the artifact was dead.

Backed up Local Storage and the 59 personal book files before advising
anything, because "should be safe" is not "is".

REPO READY, AND PUSHED
----------------------
Pre-publication audit: no keys anywhere in history, no books or PDFs
tracked, no personal paths or email, MIT, 5 MB. One real find --
.mcp.json was tracked and carried the live Supabase project ref. An
identifier, not a credential, and no key was ever committed, but it has
no business in a public repo. Untracked and gitignored.

114 commits pushed. Repo still private, deliberately: it goes public
only after he installs beta.2 himself and it works.

WHERE IT STANDS
---------------
  npm test                      clean
  build + build:beta            clean, verifier passes
  packaged-boot.cjs             6/6
  preflight.mjs                 39/39, zero external requests
  13 live suites                154 checks, 0 failures
  installer                     0.1.0-beta.2, 95.0 MB, FB094807...

TOMORROW
--------
  1. Install beta.2 from the release/ folder and use it.
  2. If it works: make the repo public, cut a GitHub Release, send the
     link with the SmartScreen warning stated up front.
  3. Then listen. BETA-TESTING-FEEDBACK.md is the place it goes.


=======================================================================
 SAME DAY, FOURTH SESSION — it installed, and it worked
=======================================================================

The install worked. That closes the last genuinely unverified thing on
the whole list, and it closes it from the right direction: he ran the
installer off GitHub, it opened, and it was fine.

Confirmed from disk afterwards -- both origins now hold saves,
localhost:5173 (dev) and file:// (installed), neither disturbing the
other. Exactly the separation predicted. The 59 personal book .txt files
sit in the shared library/ folder outside any origin, which is the part
worth backing up and now is.

BRING A BOOK IN
---------------
"The main thing we should focus on is letting people know how the
Caravan Desk works... maybe we should move that feature into the library
next to the personal shelf."

Right diagnosis, and it was placement AND naming at once. Adding a book
-- the single act this whole project is built around -- was explained
only at the Caravan Desk, three rooms away, under a name that tells a
newcomer nothing. Findable by accident.

Not moved. Split along a line that was already there: the Caravan Desk
keeps the review queue and connector imports, which are genuinely
workshop work; the everyday act comes to the Library as a station beside
Your Shelf. The name is an instruction rather than a noun on purpose --
every other station is "THE something", and this one has to answer the
question a newcomer is actually holding.

It teaches drag-the-file-onto-the-window first and loudest, then the
by-hand form, then where to get free books, then the part nobody was
ever told: which folder the text physically lands in.

THE MACS
--------
"My friends use mac." The .exe is Windows-only; the code isn't. A macOS
app cannot be built on Windows, so:

  * Tonight's answer is the website. Same Pavilion in a browser, no
    Gatekeeper, and it auto-deploys from main so it is always current.
  * .github/workflows/build-installers.yml builds both platforms on
    GitHub's own runners on any v* tag.
  * MAC-BUILD.md is the by-hand recipe for his brother, who has a Mac
    and an IDE but no AI assistant -- so it assumes no context at all.

Audited main.cjs rather than guessing, and it was in better shape than
expected: window-all-closed already checks darwin, activate re-creates
the window, nothing nulls the application menu, every path goes through
app.getPath + path.join. Three real fixes: the intake panel's hard-coded
Windows path (my own bug, hours old, and a confident lie on exactly the
machines the first testers use), arm64+x64 rather than arm64-only, and
a 1024px icon for the .icns.

AND ONE THE WEBSITE WAS HIDING
------------------------------
Checking whether the web build was a fit answer for Mac friends turned
up a Supabase host in the DEPLOYED bundle. Vercel builds with the plain
`npm run build` and had the service variables set in its dashboard, so
the public site called that project on startup -- while the README
promised, in bold, that nothing phones home.

Fixed at the root with a committed vercel.json pinning the deploy to
build:beta, so verify-beta-build.mjs now runs on every deploy and the
build FAILS rather than quietly shipping a cloud host. The guard that
protected the installer now protects the website too. The promise had
been made in one place and enforced in another; now it's enforced where
it's made.

THE PATTERN, THREE FOR THREE TODAY
----------------------------------
  morning    -- when a local model gets it wrong, ask it something
                smaller before writing a cleverer parser
  afternoon  -- test the artifact you ship, not the one you develop in
  evening    -- and that includes the website, which is also an artifact

Every one of today's serious findings came from running the real thing
and watching what it did, not from reading the code.

WHERE IT STANDS
---------------
  npm test                      clean
  build + build:beta            clean, verifier passes
  packaged-boot.cjs             6/6
  preflight.mjs                 39/39, zero external requests
  13 live suites                154 checks, 0 failures
  installer                     0.1.0-beta.2, installed and USED
  repo                          all pushed; still private by choice

TOMORROW, OR WHENEVER
---------------------
  1. Make the repo public, cut the GitHub Release, send the link.
     Windows friends get the .exe; Mac friends get the website today
     and a .dmg once his brother builds one.
  2. Then listen. BETA-TESTING-FEEDBACK.md is where it goes -- 28
     findings so far, every one of them from actually using the thing.

Good day. The thing that would have wasted his friends' evening was
invisible to every test we had, and we found it with a day to spare.


=======================================================================
 SAME DAY, FIFTH SESSION - the daily-use problem, and building the fix
=======================================================================

The most useful thing said about this project so far:

  "What I am missing is using the Pavilion daily, and why. The lesson
   planning isn't intuitive enough, Sebastian isn't enough for me to use
   daily, and I don't know where to mess around with papers."

Three complaints. They were ONE problem, and finding that was worth more
than fixing them separately.

THE DIAGNOSIS
-------------
There is no TODAY in the Pavilion. Everything is a place you COULD go;
nothing is a thing that is WAITING for you. The Writing Desk plans a day
after you fill it in, the tree holds lessons after you choose one, the
Hall dissects papers after you decide which. Every one puts the burden
of STARTING on the person. Fine for a tool you visit with an errand;
fatal for a place you inhabit, because on a tired evening "what should I
do here?" has no answer and the honest response is to close it.

Worth noting what was NOT the problem: the Library "already feels like a
precious collection." That is the hardest thing to build and it is done.
More rooms would have been more choosing.

BUILT: TODAY
------------
A door in the pause menu that says something true before you open it
("one thing is overdue", "carry on where you left off"), and behind it
at most five things, each one press from the thing itself, computed on
open from data already saved. On a real save:

  (late)     Meditation daily          was due 6 days ago
  (step)     Install Ollama            A Beating Heart, step 2 of 5
  (carried)  Finish the shelf          carried for 8 days - still want it?
  (paper)    Attention Is All You Need brought in, never pulled apart
  (unread)   Tartine Bread             2 books added and not opened

The four rules are npm test cases rather than intentions, because they
are exactly the kind that erode under later improvements: never empty
(checked against a bare Pavilion AND a fully-read one), never a wall
(twenty overdue things still yield five), one press per item, and it
notices rather than nags - a spark one day old is invisible, four days
old it asks "still want it?", and a lesson never started is never
pushed, because choosing is not continuing.

BUILT: SEBASTIAN BRINGS
-----------------------
The complaint was that he ASKS and does not BRING. So: one question -
what has to be true by tonight - and then he stops asking and carries
things in one at a time, each with a real choice. "This one is late,
sir." Do it now / keep it / let it go. The one line becomes the day's
intention; letting go genuinely clears the spark, because an observation
you cannot act on is a reminder with extra steps.

No AI anywhere in it. Fixed questions, computed items, zero network
requests - which is what makes it work for the laptop user.

BUILT: THE COMPUTER AS A TERMINAL INDEX
---------------------------------------
His idea, better than my plan. I had proposed a papers station in the
Library; he pointed at a terminal that already exists, that people
already find, and that was AI-GATED FOR NO REASON - it refused to open
without a connection, which is absurd: a terminal that cannot list your
own books unless a language model is running.

Now ls / find / papers / analyses / shelf / unread / open <n> / stats,
all over local data, plus ask which hands off to the resident and
degrades honestly. It also gave papers the front door they were missing
- the same bug the Caravan Desk had.

BUILT: RULES BEFORE THE MODEL
-----------------------------
From his own budget rule: only ask a model for what Python cannot do.
The librarian sent every book to a model in batches of eight - sixteen
calls for a 128-book import, most about books that were never ambiguous.
Now rules file first. Measured: 14 of 18 filed with NO AI and zero
network requests, and whatever nothing can place becomes a pasteable
work order for a bigger model in another window.

The tests forced a good decision: "The Tao of Physics" files to Science,
arguable but defensible, and that is fine ON PURPOSE, because nothing is
auto-applied. The safety comes from the accept step, not from any rule
being infallible.

ALSO BUILT
----------
  * Bring a Book In - the intake table beside Your Shelf, because the
    single most important action in the Pavilion was explained only at
    the Caravan Desk, three rooms away, under a name that says nothing.
  * kind:'paper' - an explicit mark, because detection was guessing from
    arxiv/doi in a source URL and a converted PDF looks like a book to
    any heuristic.
  * Coding 101, written for real - five "(planned)" steps became six
    real ones, deliberately reordered so "change one line" is step 2
    rather than a reward at the end.

THE HONEST READ ON HIS LEARNING
-------------------------------
He said his lack of full-stack skills is holding him back. Half true. In
one day he made the extraction-is-not-an-AI-job call, corrected my
trust-badge design by picking a better axis, diagnosed the Caravan Desk
as placement AND naming, settled the website/app positioning, and caught
the README sending people to a webpage instead of a download. None of
that is a full-stack skill; it is product and architecture judgement,
the rarer half.

What is actually missing is narrower: the fluency to make a small change
and verify it himself, so ideas stop having to become requests. Stages 5
and 6, and nothing else for now.

WHERE IT STANDS
---------------
  npm test                      clean
  preflight.mjs                 39/39, zero external requests
  packaged-boot.cjs             6/6
  installer                     0.1.0-beta.2, rebuilt to match the code
  repo                          all pushed; still private by choice

TOMORROW
--------
  1. USE IT. Open Today on an ordinary tired evening and see whether
     there is something worth ten minutes without having to decide what.
     That is the only test that matters now, and nothing else on the
     list should be built before the answer is known.
  2. Coding 101, steps 1-2. Fifteen minutes.
  3. If the repo goes public: cut the Release, send the link.
