=======================================================================
 SAND PAVILION — dev log, 2026-07-28
 "One question at a time"
=======================================================================

The whole session came out of one sentence:

  "We have a pathway for Claude, but what human needs are simple steps.
   If it's too much for the model we should break up the steps to make
   it more manageable for the AI and the user."

Yesterday's round ended with the lesson-drafting *working* but weak: the
real local model returned a single run-on line and a deliberately
forgiving parser rescued something out of it. I wrote at the time that
"a 9B model's format adherence is unreliable" and treated that as a fact
of life to be absorbed. It wasn't. It was a symptom of asking too much
in one breath.

WHAT WAS ACTUALLY WRONG
-----------------------
One call asked ornith:9b for TITLE, SUMMARY, TRACK, LEVEL and 4-6 STEPS.
It answered the shape it could hold, not the shape it was given. The
live test had been recording this plainly for a day — `steps: 1 lines`,
three checks red — and I'd read it as a parsing problem.

THE FIX, WHICH IS THE SAME FIX FOR BOTH HALVES
----------------------------------------------
The draft is a chain of single questions now: a title, then a summary,
then a track, then each step in turn, each one a one-line answer with
the steps written so far in front of it.

The model finds every ask easy. And the person watching gets simple
steps too, which is the part I would have missed if the request hadn't
named both: the form fills in field by field, the status says "6 of 8"
instead of spinning, there's a Stop button that keeps whatever arrived,
and a stage that comes back badly costs that one field instead of the
whole draft.

Same prompt, real model, before -> after:
    steps drafted        1  ->  5
    live checks passing  7/11 -> 11/11

TWO MORE THINGS ONLY THE REAL MODEL COULD SHOW
----------------------------------------------
Once the shape was fixed, the content faults became visible:

  * It repeated itself. Given steps 1-4 and asked for step 5, it
    re-wrote step 3 -- "Folds hands into a loose fist" then "Folds hands
    into a fist". Exact-match dedupe is useless against that. So
    tooSimilarStep() compares the ACTION half only, drops filler words,
    and calls it a repeat when most of the shorter step's words appear
    in the other. The chain then just asks again; three spare asks are
    budgeted for exactly this.

  * It wrote in the third person -- "Sits comfortably with back
    straight". Fixed in the prompt: address them directly, start with a
    verb.

Both are permanent smoke cases now, with the real captured strings.

THE LIBRARIAN GOT THE SAME MEDICINE
-----------------------------------
Sixty titles in one ask -> eight at a time. That was precisely the
"it answered, but nothing matched your titles closely enough" failure
from yesterday: a small model given a long list files the first few,
invents shelves for the middle and drops the rest. Each batch's picks
are kept even if a later one fails, and the status reports a real count.
Real-model result: 3 of 4 -> 4 of 4.

THE ONE THAT WASN'T ASKED FOR
-----------------------------
While chasing a stray console error in the grove suite I probed the
running app's actual network traffic, and found it calling

    https://<project>.supabase.co/rest/v1/library_documents

on startup, unprompted, every launch.

That is correct for this dev instance and completely wrong for anything
handed to a beta tester. .env.beta already empties every service
variable, so `build:beta` was always clean -- the real hazard was that
ONE mistyped script (`electron:build` instead of `electron:build:beta`)
produces a poisoned installer that looks identical to a good one.

So it's structural now: scripts/verify-beta-build.mjs runs inside both
beta build scripts and FAILS THE BUILD if the bundle carries a Supabase
host, a JWT-shaped string, a Supabase key or MinIO credentials. Proved
both directions -- it rejects a dev build (naming the exact key it
found) and passes a beta build.

Nothing was ever committed; dist/ and .env.local are both untracked.
This was a shipping hazard, not a leak. But it's the kind of thing that
would have been discovered by a tester, which is the wrong person to
discover it.

WHAT THIS SESSION IS WORTH REMEMBERING FOR
------------------------------------------
When a local model gets something wrong, ask it something smaller before
you write a cleverer parser. It is nearly always the better interface
for the human as well -- which is what the request said in the first
place, and I only half heard it.

Also: run the thing, watch its traffic, don't just read it. Both of
today's real findings came from observation, not from code review.

VERIFIED
--------
  npm test                      clean
  npm run build                 clean
  npm run build:beta            clean + verifier passes
  verify:beta on a dev build    correctly FAILS
  13 live puppeteer suites      164 checks, no failures, no page errors
  test-ai-real.mjs              11/11 against real ollama/ornith:9b

STILL OPEN (unchanged)
----------------------
  * The user's own desktop-app run (BETA-PREFLIGHT tiers 0-2)
  * The welcome-packet decision
  * Cut the 0.1.0-beta.2 installer
  * Clean-machine install test
  * #10 -- Course Board / tree as a college index (deferred past beta)


=======================================================================
 SAME DAY, SECOND SESSION — "we are at a good point to cut the beta"
=======================================================================

Answer: we were not, and the reason was the worst one available.

THE PACKAGED APP HAD NEVER BOOTED
---------------------------------
Chasing whether bundled book texts would load in the desktop app, I
booted dist/ the way the INSTALLER boots it -- win.loadFile(), a file://
origin -- instead of the way development does. It didn't boot. There was
no vite.config.js in the project at all, so Vite emitted absolute asset
paths (/assets/index-xxx.js), and under file:// those resolve to the
filesystem ROOT: C:/assets/... Nothing there.

The failure is quiet in exactly the way that keeps a bug alive for
weeks. index.html itself loads, so the window shows the title screen and
"Enter the Grounds". Behind it: no script, no stylesheet, no error
dialog. A button that does nothing.

  index.html loads at all      : true
  THE GAME'S OWN CODE RUNS     : false
  the stylesheet arrives       : rgba(0,0,0,0)

Every desktop check until now went through electron:dev, which loads
from the vite dev server over http and therefore cannot see this. The
one gap that mattered most, hidden by the one convenience that made
testing easy. The 11 July beta.1 installer was almost certainly a dead
app -- which fits the record exactly: the clean-machine install test is
the one thing that was never done.

Fixed with base:'./'. Relative paths work under file:// and http alike,
so Vercel is unaffected (39/39 web pre-flight still green). Guarded by
test/live/packaged-boot.cjs, which boots dist/ through a real Electron
main process using the installer's own preload, sandbox and loadFile,
and fails if the code didn't run, the stylesheet didn't arrive, the
bridge isn't wired, Start doesn't start, or a panel comes up empty.

THEN THE ARTIFACT ITSELF, ONCE ANYONE LOOKED
--------------------------------------------
  * The build log had been saying "default Electron icon is used" for
    weeks. Every tester's taskbar would have shown the Electron atom.
    There was no icon asset at all. Drew one instead of downloading one:
    scripts/make-icon.mjs renders a 32x32 pixel-art pavilion (moon
    behind, dune below), scales it by whole numbers to 256/128/64/32,
    encodes the PNGs by hand with node:zlib and wraps them in an ICO. No
    dependencies, colours straight out of style.css. Integer scaling is
    the point -- it stays crisp at every size Windows asks for.

  * The shipped app.asar was 521 node_modules entries to 5 dist ones.
    electron-builder packs everything in `dependencies`, and
    supabase-js was listed there even though the Electron main process
    never requires it. It's a bundle-time dependency. Moved to
    devDependencies: 530 entries -> 9.

AND THE LAST HONEST GAP IN THE LIBRARY
--------------------------------------
Ten of the seed books -- Meditations, The Republic, the Tao Te Ching,
Origin of Species and six more -- ship as a Pavilion SUMMARY. They had
no "Read the full text" button and said nothing about why, so opening
Marcus Aurelius gave an unexplained precis and no way to tell whether
the app was broken or that was all there was. Silence is not honesty.
Each now says plainly: this is the summary, the text is public domain,
here is the source, drag the file onto this window and this page becomes
the real book. The Pavilion's own seven writings never show it -- they
are complete as they stand and aren't missing anything.

beta.2 IS CUT
-------------
release/Sand Pavilion Setup 0.1.0-beta.2.exe, 94.97 MB, and it is the
first installer ever verified as an artifact rather than as source.

THE THREAD RUNNING THROUGH BOTH SESSIONS TODAY
----------------------------------------------
Morning: when a local model gets it wrong, ask it something smaller.
Afternoon: test the artifact you ship, not the one you develop in.

Both of today's worst findings -- the Supabase call on startup and the
dead packaged app -- were invisible to every existing test and obvious
within minutes of running the real thing and watching what it did.

VERIFIED
--------
  npm test                      clean
  npm run build:beta            clean + verifier passes
  packaged-boot.cjs             6/6, real Electron 43, file://
  preflight.mjs                 39/39, no external requests
  app.asar                      9 entries, no keys, no cloud host

STILL OPEN
----------
  * The clean-machine install test (genuinely un-automatable)
  * Ten unbroken minutes listening to a real book
  * Copyright triage over the real 128 books
  * #10 -- Course Board / tree as a college index (deferred past beta)
