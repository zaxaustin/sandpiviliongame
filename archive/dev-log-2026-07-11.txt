dev log — 2026-07-11
(the day Sebastian got his calendar — the "last key before beta" v1 gone
code-complete)

Full detail here so README.md can stay a current-state reference. Session
opened by going through the plans and the beta-feedback log end to end
against the actual code, to decide what genuinely gates beta before
building anything.

================================================================
PART 1 — the beta map, checked against reality (not just the last note)
================================================================

Read README, all 15 plan docs' gating ones (BUTLER-SEBASTIAN,
BETA-TESTING-FEEDBACK, AI-INTEGRATION-NOTES), and the 2026-07-10 dev-log.
The previous session's closing path named three real beta gates: finish
Sebastian, an AI-reliability pass, and a clean "someone else can run this"
path. Confirmed all three against the code, and corrected one thing the
last note got optimistic about:

- The Monk's model-selection fix (bestLocalModel) was logged as "in
  progress by the user." It has NOT landed. Read provider.js:78-87 — it
  still just picks the largest-parameter installed model, no thinking-model
  exclusion. So the single biggest named failure mode (flagship resident
  hands a beta tester a qwen3.5/deepseek-r1 that hangs or returns blank) is
  still live. Small, contained fix; moved up the list. Named honestly in
  README's "still open before beta."

User chose to build Sebastian's calendar first (his named "last key").

================================================================
PART 2 — Butler Sebastian, v1 steps 3-6 (the calendar half)
================================================================

BUTLER-SEBASTIAN-PLAN.md steps 1-2 (his charter + chat presence) shipped
2026-07-10. Built the back half this session:

- DATA MODEL (step 3). calendar:[] added to freshData() in entities.js,
  with the same migration patch the other newer fields use (older saves
  read as empty — no migration needed). An event is { id, date, start,
  end, title, note }. Local, in the same save, exported/imported with
  everything else.

- ONE SOURCE OF TRUTH (step 5). upcomingItems() now folds in calendar
  events, so the HUD due badge, the Upcoming panel, and Sebastian's read
  all draw from one place. The honest part, following the archived-course
  precedent: only TODAY-OR-LATER events surface, so a past event never
  falsely nags as "overdue" the way a missed course deadline honestly
  should. Same-day items sort by start time. Verified the badge math: an
  event bumps the glanceable count but (being never < today) can never add
  to the "overdue" count — exactly right.
  Updated every upcomingItems consumer for the new 'event' kind: a shared
  upcomingIcon() helper (📚 course / 📅 event / 📝 grant), the Writing
  Desk's due line, and the Upcoming panel (clicking an event now opens the
  calendar on that day).

- CALENDAR UI + HIS DESK (step 4). A new #calendarOv overlay, openCalendar()
  in overlays.js: a month grid (prev/next month, today highlighted, a dot
  count per day) → click a day → a day/agenda view listing that day's
  events with a remove button and an add-event form (title, optional
  start/end time, optional note). A small .calgrid/.calcell CSS block added
  to style.css (plus themed date/time inputs). A 'calendar' station stands
  at Sebastian's desk (10,7) in buildWorkshop, with its own drawStation
  case in render.js (a red-banded desk pad on a stand) and an onAction
  dispatch in main.js — the exact Records Hall pattern. Two doors, one
  spot: the NPC for the conversation, the desk for the grid, plus a
  "📅 Open the calendar" button inside his chat.

- ADVISORY READ (step 6). butlerDayRead() (his AI grounding) now speaks to
  today's timed events directly. A new butlerAdvisoryLine() computes the
  overbooked / empty / neglected / well-kept state as one plain-text line
  in his own butler voice, shown at the top of the calendar — and computed
  with NO AI connected at all, so the advisory works for everyone and just
  gets richer when a model is present. A priority ladder (overdue >
  neglected sparks > overbooked > empty > well-kept), the most worth-saying
  thing said once. Computed only on opening the calendar — never a timer.

Every onclick handler registered in the Object.assign(window,{...}) export
block (the documented silent-fail gotcha). 'calendar' added to the smoke
test's KNOWN_STATION_KINDS list.

================================================================
VERIFICATION — static, plus a real date-math test; honest about the gap
================================================================

- node --check clean on all five touched JS files (caught and fixed one
  template-string escaping slip along the way).
- npm test (smoke) passes — 9 scenes, all station kinds known, no overlaps.
- npm run build clean (the >500kB chunk warning is pre-existing).
- A standalone Node test of the calendar date math: leap-year Feb (2024=29,
  2026=28), month-boundary wraps both directions, the weekday the 1st falls
  on (Jul 1 2026 = Wed, Jan 1 2026 = Thu), and tomorrow crossing a month
  end (Jul 31 -> Aug 1). ALL OK.

NOT done: a live click-through in a running browser/Electron window. The
user prefers no Chrome/Playwright harness be pulled in, so that final
"walk up to the desk and actually add an event, watch the badge tick"
pass is the one thing left — best done by the user in npm run electron:dev.
Said plainly in the plan and the README rather than claimed as verified.

================================================================
WHERE THINGS STAND
================================================================

Sebastian v1 is code-complete. The two remaining beta gates, both named
honestly in README's "still open before beta":
  1. AI reliability — the Monk's bestLocalModel() fix (still a live gap in
     provider.js), plus a once-over of the empty-reply/timeout fallbacks
     for a stranger's shakier setup.
  2. A clean "someone who isn't Zac can run this" path — first-arrival
     orientation + a clean-machine install verification.
Also still open, smaller: the Computer/Sebastian merge decision
(recommended: fold the Computer in), and full text for the 5 newest books
(a push-fulltext.py run each, Docker/MinIO up).

================================================================
PART 3 — a direction conversation, captured into two new plans
================================================================

The user asked about the Monk model issue (they'd delayed the fix, honestly,
for lack of AI background). Explained it plainly — biggest installed model
!= most reliable; the biggest is often a "thinking" model that hides a long
reasoning pass and can hang or return blank; so the flagship resident is the
most exposed to the worst first impression. Then the user steered the
resolution somewhere better than "exclude thinking models":

- The Monk is DEFINITELY in the beta — "what people are missing is
  guidance," and he's the one who gives it. Don't hold him back.
- The real pre-launch task: "pick a series of models he works well with on a
  variety of different computers — then we can truly beta launch." I.e.
  curate a TIERED set (modest/mid/beefy machine), each vetted for guidance
  quality AND real latency/reliability on that hardware class.
- Users install/assign models per their own machine (the "customize plan for
  beta"). Per-resident model routing does NOT exist yet (detectAI picks one
  connection for everyone) — a real new capability the customization needs.
- Keep the Pavilion SMALL, with clear outlets to link your own local AI; a
  better coder forks their own setup anyway. Zac's contribution is the game-
  like interface for real work ("our bread and butter"); visitors get a
  welcome packet of books and clear places to add their own AI, and fill the
  Pavilion themselves just as we have. Learning the AI tradeoffs is so the
  less-techy testers can actually be HELPED, not just shipped at.
- The "log room": the Records Hall today shows the PAVILION's hand-kept dev
  history (PAVILION_TIMELINE), not the visitor's notes, and doesn't read the
  dev-logs live. What the user wants is the visitor's own notes — scattered
  today across five stores (My Notes/data.notes, bookNotes, chatNotes,
  research notes, grant docs) — gathered and organised for the long term,
  with Sebastian as concierge. Likely repurpose the Records Hall to hold it.

Captured all of it into two new plans so it survives the night:
  - plans/BETA-LAUNCH-PLAN.md — the beta stance + the Monk tiered-model /
    per-machine customization resolution + welcome packet + fill-it-yourself.
  - plans/NOTES-AND-LOG-ROOM-PLAN.md — the honest Records Hall state, the
    five scattered note stores, and a small "gather + organise + Sebastian"
    plan (a view over existing notes, not a fifth store).
Memory updated: feedback-monk-model-priority (the tiered-set refinement) and
a new project_beta_launch_shape. README's changelog + Sebastian section and
this log updated to match. No code written in Part 3 — direction and plans
only, by the user's call to wind down for the night.

PICK UP HERE IN THE MORNING (user's explicit intent — "right where we left
off"): nothing half-built or broken. Natural next moves, in the user's own
priority: (1) the Monk's tiered-model curation — the named "truly beta
launch" task (BETA-LAUNCH-PLAN.md #1), pairs with checking what models are
actually installed on this machine; (2) live click-through of Sebastian's
calendar in npm run electron:dev (the one unverified piece); (3) the
gather-your-notes view (NOTES-AND-LOG-ROOM-PLAN.md step 1). All statically
clean as of tonight: node --check, npm test, npm run build all pass.

================================================================
MORNING SESSION (2026-07-11 cont.) — the AI-personality pass, the
library protocol, and live streaming
================================================================

The user tested overnight; "super happy overall," questions mainly about
what the AIs are preloaded with. Left detailed testing notes at the end of
BETA-TESTING-FEEDBACK.md, triaged into "Round 4" (#26-35).

PART A — the AI grounding/personality pass (guidelines, not restrictions):
- KEY finding: the Mountain Monk had ZERO grounding — his whole grasp of the
  dharma ran on the base model's training alone. And the user had seen his
  thinking literally say `check constraints: "Speak briefly and warmly"` —
  because his prompt was a behavioural rulebook a thinking model audits
  itself against. Both fixed: he's now grounded in the real Library shelf
  list (like Quill) PLUS in-prompt canon essentials (Four Noble Truths, the
  eight folds, the three marks, the anatta formula, karma as cetana), and
  his prompt was rewritten as identity + knowledge rather than a checklist.
  Dropped the traffic-cop routing (reverses the older #16 "actively redirect"
  call — confirmed the user has evolved past it). Given a hidden name he
  offers only in a true moment: Leaf Wind / ch'il naa'i (Apache — spelling
  UNVERIFIED, flagged honestly, used as the user wrote it).
- Quill lightened: performance stripped, a plain knowledgeable librarian
  whose catalogue-lookup + plan-drafting are offered, never forced. Kept the
  one hard "never invent a book not on the shelves" line (Library integrity,
  not personality).
- Captured the principle as memory feedback_prompts_are_guidelines: prompts
  ENABLE via identity+knowledge+grounding, never a fear-based leash.

PART B — the Library "protocol" (the user wrote "protocol" repeatedly in
beta notes; wants it documented for themselves and the README):
- Investigated the whole pipeline (tools/caravan/, library-inbox/,
  library-drafts/, the in-game Caravan Desk). It was already solid but
  scattered across three files + the desk, with no single authoritative
  how-to. Wrote PROTOCOLS.md — the canonical manual: Protocol 1 (adding a
  book, BOTH pathways: in-game .txt drag-drop, and the terminal
  draft→promote→push-fulltext pipeline) with the provenance-first rule and a
  free-source index table up front; Protocol 2 (connecting your own AI);
  and a "more to come" stub (shelves/traditions, bundles, own-Pavilion).
  README now points at it; fixed a stale "four shelves" line in
  library-drafts/README (it's nine now).

PART C — live streaming for the Monk (the user: "do the real fix"):
- provider.js: Ollama provider now supportsStream:true; chat() takes
  opts.onStream and, when present, streams stream:true NDJSON via a new
  ollamaStreamChat(), accumulating message.content + message.thinking and
  calling back as they grow. Any failure falls back to the buffered request
  — no regression; cloud providers untouched (local-only).
- The desktop bridge buffered (would defeat streaming), so added a real
  streaming IPC channel: fetchStream in preload.cjs -> desktop-fetch-stream-
  start in main.cjs (getReader loop, per-request channel id, localhost-only
  guard kept). Falls back if unavailable.
- UI (overlays.js): sendChatMessage streams for any local resident; a live
  bubble shows the reasoning open above the answer, both patched in place per
  token (updateStreamingBubble), a blinking caret; the phone "thinking…"
  state respects streaming; the streamed thought is kept even if the final
  answer is empty (the "small win" too). If it streamed, the final render
  skips the typewriter.
- VERIFIED: node --check on all changed files (incl. the two .cjs), npm test,
  npm run build all clean; plus a standalone test of the NDJSON chunk-parser
  across pathological byte boundaries (1,3,7,13,50,10000-byte chunks; partial
  JSON held until its newline) — ALL OK. NOT verified: the actual on-screen
  stream in a running browser/Electron window — the user's to run (no browser
  harness pulled in, by their standing preference). The Electron streaming
  bridge especially wants a real electron:dev check.

Still open after this session: verify streaming live; the Monk tiered-model
curation (BETA-LAUNCH #1); Sebastian calendar live click-through; the
gather-your-notes view; and the bigger noted ideas (breviary, Lotus Sutra,
mega-snowball bundles, second Library floor).

================================================================
SESSION (later, 2026-07-11) — THE BETA BUILD, ACTUALLY BUILT
================================================================
Direct instruction: stop planning, build the beta per BETA-BUILD-PLAN.md,
own branch, free/self-built over third parties, user reviews as we go.
All work on the new `beta` branch, main untouched.

Phase 0 decisions (delegated, now locked):
- Storage: personal books' full text = plain .txt files in Electron's own
  userData/library folder via the existing desktop bridge; browser build
  falls back to inline-in-save (with an honest ~1.5MB refusal for books
  too big for localStorage). No Docker, no MinIO, nothing to install.
- Welcome packet: the current 21-text seed ships as-is (4 full books
  inline + the essays/summaries) — the middle path between "empty" and
  "cathedral"; testers fill the rest themselves.
- Provenance: user-added books are `personal:true`, tradition 'Personal',
  marked 👤 everywhere, never mixed into the six certified shelf blocks.
- Backend: none at all. Supabase stripped from the beta build by mode.

Built (Phase 1 — the personal library, the one real engineering task):
- data.personalLibrary[] (freshData + migration); Store gains
  registerPersonalDocs(getter) and merges personal docs into
  allDocs/listDocs/getDoc — so the Index, shelves, and Quill's grounding
  all see personal books for free, no call site changed.
- electron/main.cjs: desktop-library-write/read/delete IPC handlers,
  slug-locked filenames (no path traversal), files under
  app.getPath('userData')/library/. preload.cjs exposes
  libraryWrite/libraryRead/libraryDelete.
- The Caravan Desk queue: every item gets "👤 Shelve on Your Shelf" —
  the whole local path (writes the file on desktop / inlines in browser,
  builds the catalog card from the item + any AI-drafted summary, removes
  it from the queue). This is what the approve→batch→terminal pipeline
  never offered a tester: a book in, readable, zero infrastructure.
- The Reader: openFullText handles {storage:{personal:...}} via the
  bridge; personal books show a 👤 badge + "Remove from Your Shelf"
  (which also deletes the stored file, best-effort).
- The world: a YOUR SHELF station (kind 'yourshelf') in the Library by
  the reading nook — small warm-wood case, half-empty on purpose; opens
  openShelf('Personal'); Quill's intro line mentions it; empty-shelf text
  points at the Caravan Desk. smoke.mjs knows the new kind.
- Fixed en route: the title screen's MinIO health check now only fires
  for {bucket,key} pointers — a personal file pointer must never scare a
  tester with a "MinIO not running" warning.

Built (Phase 2 — local-only build mode):
- .env.beta (committed on purpose: it EMPTIES the service vars; Vite
  gives mode files priority over .env.local, so the dev machine's own
  config stays put and still never reaches an installer).
- package.json: build:beta + electron:build:beta scripts.
- VERIFIED: the beta dist/ has 0 mentions of the Supabase project ref,
  0 of the publishable key, 0 of supabase.co. Nothing to strip by hand;
  every no-backend surface already degrades with an honest message.

Built (Phase 2b — first arrival):
- A welcome overlay (#welcomeOv): keys, the three doors, "make the
  residents talk" pointing at Ollama + the in-world protocol book,
  "nothing phones home." Auto-shows exactly ONCE, only for a genuinely
  fresh save (no data.pos yet, seenWelcome flag), then lives behind a
  new "🧭 New here? Start here" title-screen button forever. The
  standing automation rule applied to orientation itself.

Built (Phase 3 — the model on-ramp, BETA-LAUNCH #1):
- PROTOCOLS.md Protocol 2: a real tiered pull guide (modest/everyday/
  capable/strong), the thinking-vs-instruct tradeoff stated plainly, the
  two Pavilion facts (the Monk claims the largest model installed — his
  depth is never traded away; everyone else favors speed). The desktop
  app's "skips OLLAMA_ORIGINS entirely" fact written down at last.
- The Connections panel: a matching 📏 "Which model fits my machine?"
  collapsible, one glance's worth.
- Protocol 1 Pathway A now ends at "👤 Shelve on Your Shelf" with the
  provenance split stated plainly (certified = a steward vouched;
  personal = you vouched; both first-class, the label always honest).
- bestLocalModel() deliberately NOT changed — the standing Monk rule
  holds; guidance, not downgrade, was always the resolution.

Verified: node --check on every touched file; npm test (smoke, incl. the
new station kind); npm run build:beta clean; the dist/ leak grep above.
electron-builder packaging run started (see next entry / README).
NOT verified, stated honestly: a live click-through of the shelve→read
flow in a running window, and the clean-machine install test — both
remain the user's (and the plan's §6.8) to run.

Addendum (same session): MANUAL.md written — the visitor's handbook the
protocols were missing a layer under (named directly by the user: "there
needs to be a user manual to get familiar with the sand pavilion").
Covers controls, the map, the three residents' division, the Library/
Study/Workshop room guides, the AI setup pointer, where data lives
(including the honest export limit: desktop-shelved personal book FILES
don't travel inside a save export), and a symptom table. README's
"Running it" rebuilt as "every mode, and how to get each up" — the five
modes (browser dev, static build, electron:dev, installed app, shareable
beta) plus the three optional layers (Ollama, MinIO, Supabase), each with
what it needs. MANUAL.md linked from the orientation map and "New here?".

Addendum 2 (same session): backend decided by the user — keep Supabase for
now; when outgrown, the hybrid (small VPS running the same Postgres+MinIO
Docker stack the dev machine already runs, Supabase kept for the social
pieces). Recorded in BETA-BUILD-PLAN.md §5. README Mode 5 now says where
the installer actually lands (release/) and carries the one real caution:
the beta installer and the dev machine's own Mode-4 install are the same
app to Windows (same identity, same save) — installing the beta on the
dev machine would replace the full personal app with the local-only one
(recoverable, nothing lost, but the right way to *try* the beta here is
the unpacked exe: release/win-unpacked/Sand Pavilion.exe).

Addendum 3 (same session): beta merged into main (fast-forward, dd57aeb) —
one codebase, two build commands; the branch never forked the app. Then
the documentation wash the user asked for: README's What's-next section
had become a ~300-line changelog of "Done since then" blocks — excised
(the dev logs already carry all of it) and replaced with a compact
"How it got here" + a real near-gates list (live click-through, clean-
machine test, welcome-packet decision, export-files gap, then testers)
+ the recorded backend decision. Stale facts fixed: Library count (49
live / 21 seed, Your Shelf added), the local-first invariant now names
personal-book file storage, Local-AI-panel item corrected to steps 1-3
built. Plans triaged: BUTLER-SEBASTIAN and BOOK-DRAG-DROP closed and
moved to plans/done/ with closure banners; STATUS banners stamped on
BETA-LAUNCH (built; stance stays), DESKTOP-APP (phases done + beta
installer; clean-machine gap unchanged), LIBRARY-SCALING (the three
storage layers reconciled so they stop reading as contradictions),
NOTES-AND-LOG-ROOM (core built; Records Hall repurpose open),
LOCAL-AI-MONITORING (steps 1-3 built), COURSE-BOARD (stage 1 built),
AI-INTEGRATION-NOTES (both named risks resolved: streaming built, Monk
model closed as guidance-not-downgrade). All cross-references checked
after the moves — zero stale links. One self-inflicted lesson logged
honestly: a PowerShell edit first ran with unset variables (shell state
does not persist between calls) and then mojibake d the README's UTF-8
(PS5.1 Get-Content defaults to ANSI) — both caught immediately, file
restored from git, redone atomically with explicit UTF-8 round-trip.
